
class MMTFileManager:
    @staticmethod
    def getLines(id, historyDayCount, beforeDate):
        # 这里只是简单返回一个空列表，实际需要根据具体逻辑实现
        return []

    @staticmethod
    def getTicks(id, historyDayCount, beforeDate):
        # 这里只是简单返回一个空列表，实际需要根据具体逻辑实现
        return []

    @staticmethod
    def getM1Dir(id):
        # 这里只是简单返回一个目录名，实际需要根据具体逻辑实现
        return f"{id}_m1_dir"